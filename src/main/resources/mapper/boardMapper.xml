<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
    <insert id="save" parameterType="board">
        insert into board_table(boardWriter, boardPass, boardTitle, boardContents,uuid)
        values(#{boardWriter}, #{boardPass}, #{boardTitle}, #{boardContents}, #{uuid})
    </insert>
    <select id="findAll" resultType="board">
        select * from board_table order by id desc
    </select>
    <update id = "updateHits" parameterType="String">
        update board_table set boardHits = boardHits + 1 where uuid = #{uuid}
    </update>
    <select id="findById" parameterType="String" resultType="board">
        select * from board_table where uuid=#{uuid}
    </select>
    <update id="update" parameterType="board">
        update board_table set boardTitle=${boardTitle}, boardContents=${boardContents}
        where uuid=#{uuid}
    </update>
    <delete id="delete" parameterType="String">
        delete from board_table where uuid=#{uuid}
    </delete>
</mapper>